contract('VotingTokenFactory', function(accounts) {
  it("should create for and against tokens array", function(done) {

    // refactor to two factory classes
    
    done();
    //
    // var factory = VotingTokenFactory.deployed();
    //
    // return factory.createVotingTokens.call().then(function(sender) {
    //   console.log(VotingForToken.at(sender).balanceOf.call(sender));
    //   // console.log(factory.created.call());
    //   // assert.equal(balance.valueOf(), 10000, "10000 wasn't in the first account");
    // });
    // VotingTokenFactory.new().then(function(instance){
    //
    //   VotingTokenFactory.at(instance.address).createVotingTokens.call().then(function(sender){
    //
    //
    //     // console.log(VotingTokenFactory.at(instance.address).created.call()[sender]);
    //     //
    //     //
    //     // //var addresses = VotingTokenFactory.at(instance.address).created[sender];
    //     //
    //     // VotingForToken.at(sender).balanceOf.call(sender).then(function(balance){
    //     //   var balanceAsNumber = balance.toNumber();
    //     //   console.log(balanceAsNumber);
    //     //   assert.equal(balanceAsNumber, 100, "100 for tokens not in first account");
    //     // });
    //   });
    //
    //
    //   VotingTokenFactory.at(instance.address).created.call().then(function(map){
    //     console.log(map);
    //
    //   });
    //   // console.log(created_map);
    //
    // });

  });
});
