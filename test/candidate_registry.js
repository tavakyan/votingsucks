contract('CandidateRegistry', function(accounts) {
  it("should assert true", function(done) {
    // var candidate_registry = CandidateRegistry.at(CandidateRegistry.deployed_address);
    assert.isTrue(true);
    done();
  });
});
